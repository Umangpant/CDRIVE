spring.application.name=Ecomm
# ------------------- MySQL Configuration -------------------
spring.datasource.url=jdbc:mysql://localhost:3306/ecomm?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=KILO
# ------------------- JPA/Hibernate Settings -------------------
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.sql.init.mode=always
server.port=8080
# OLD H2 LINES (KEEP THEM COMMENTED OUT OR REMOVE)
# spring.jpa.defer-datasource-initialization=true
# spring.datasource.url=jdbc:h2:mem:DB:ecomm
#spring.jpa.defer-datasource-initialization=true  "When we use an H2 database and run our project, Spring might try to connect to a table before JPA has finished creating it. This setting tells Spring to wait, so it only connects after the data and tables are fully loaded, preventing errors."